import { styled } from "../../stitches.config";

const Label = styled("label", {
  display: "flex",
  fontSize: "10pt",
  marginLeft: "5px",
  fontWeight: "bold",
  flexDirection: "column",
});

export default Label;
